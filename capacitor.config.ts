import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gdesast.certificaciones',
  appName: 'certificaciones.app',
  webDir: 'dist/Proyecto-Certificaciones/browser'
};

export default config;
